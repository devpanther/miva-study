# Thursday — MTH_102 nightly check

*Problems-only session on Week 1.*
*12 questions, straight after the hour. Score out of 12.*

**1.** f(x) = x² − 4. Find and simplify f(a + 1).
A. a² − 3
B. a² + 2a − 3
C. a² + 2a + 1
D. a² + 2a + 5

**2.** Find the domain of f(x) = (x + 1)/(x² − 5x + 6).
A. ℝ \ {2, 3}
B. ℝ \ {−2, −3}
C. ℝ \ {−1, 2, 3}
D. ℝ \ {6}

**3.** Find the range of f(x) = (2x + 1)/(x − 3) by making x the subject.
A. (−∞, ∞)
B. ℝ \ {3}
C. ℝ \ {2}
D. ℝ \ {−1/2}

**4.** f(x) = 2x − 3 and g(x) = x² + 1. Evaluate (g ∘ f)(3).
A. 17
B. 30
C. 10
D. 13

**5.** f(x) = 3x − 2 and g(x) = x². Find (g ∘ f)(x) in expanded form.
A. 9x² + 4
B. 3x² − 2
C. 9x² − 6x + 4
D. 9x² − 12x + 4

**6.** f(x) = 3x − 7. Find f⁻¹(5).
A. 8
B. 4
C. −2/3
D. 1/8

**7.** Find the domain of f(x) = √(4 − 2x).
A. [2, ∞)
B. [−2, ∞)
C. (−∞, −2]
D. (−∞, 2]

**8.** For f(x) = (x² − 9)/(x − 3), a student cancels to x + 3 and states the domain as ℝ. What is the correct domain of f?
A. ℝ \ {3}
B. ℝ
C. ℝ \ {−3}
D. ℝ \ {−3, 3}

**9.** Find the domain and the range of h(x) = √(x + 6) − 4.
A. D = [6, ∞), R = [−4, ∞)
B. D = [−6, ∞), R = [0, ∞)
C. D = (−6, ∞), R = (−4, ∞)
D. D = [−6, ∞), R = [−4, ∞)

**10.** Find the inverse of f(x) = (3x − 2)/4.
A. f⁻¹(x) = (4x + 2)/3
B. f⁻¹(x) = (4x − 2)/3
C. f⁻¹(x) = 4/(3x − 2)
D. f⁻¹(x) = (3x + 2)/4

**11.** Find the domain of f(x) = √(x − 1)/(x − 4).
A. [1, ∞)
B. ℝ \ {4}
C. [1, 4) ∪ (4, ∞)
D. (1, 4) ∪ (4, ∞)

**12.** The range of h(x) = 2 − |x − 3| is (−∞, 2]. Why is the bracket at 2 square while the bracket at −∞ is round?
A. A square bracket always marks a maximum and a round bracket always marks a minimum
B. y = 2 is attained, at x = 3, while −∞ is not a number and is never attained
C. h is defined at x = 2 but is undefined at x = −∞
D. y = 2 is never quite reached, while the values run all the way down to −∞

---

## Answers

**1. B** — *Evaluating a function at an algebraic expression.* Replace every x by (a + 1): (a + 1)² − 4 = a² + 2a + 1 − 4 = a² + 2a − 3.

a² − 3 squares term by term and drops the 2a cross term; a² + 2a + 1 expands the square but forgets the −4; a² + 2a + 5 adds 4 instead of subtracting it.

**2. A** — *Domain of a rational function by factoring the denominator.* Set the denominator ≠ 0: x² − 5x + 6 = (x − 2)(x − 3), which is zero at x = 2 and x = 3, so D = ℝ \ {2, 3}.

ℝ \ {−2, −3} has the signs of the roots backwards; ℝ \ {−1, 2, 3} also excludes the zero of the numerator, which never restricts a domain; ℝ \ {6} excludes the constant term instead of solving the quadratic.

**3. C** — *Range of a rational function by making x the subject.* y = (2x + 1)/(x − 3) gives y(x − 3) = 2x + 1, so xy − 2x = 3y + 1 and x = (3y + 1)/(y − 2). This is a real number for every y except y = 2, so R = ℝ \ {2}.

(−∞, ∞) forgets the division by y − 2; ℝ \ {3} is the domain, not the range; ℝ \ {−1/2} is the x-intercept mistaken for a missing output.

**4. C** — *Evaluating a composite at a number.* g ∘ f means f runs first: f(3) = 2(3) − 3 = 3, then g(3) = 3² + 1 = 10.

17 is (f ∘ g)(3), the wrong order (g(3) = 10, then f(10) = 17); 30 multiplies f(3) and g(3); 13 adds them.

**5. D** — *Expanding a squared composite.* g ∘ f = g[f(x)] = (3x − 2)² = (3x − 2)(3x − 2) = 9x² − 6x − 6x + 4 = 9x² − 12x + 4.

9x² + 4 squares each term separately and loses both cross terms; 3x² − 2 is f ∘ g, the other order; 9x² − 6x + 4 keeps only one of the two −6x terms.

**6. B** — *Evaluating an inverse at a number.* f⁻¹(5) is the input that f sends to 5: 3x − 7 = 5, so 3x = 12 and x = 4. Equivalently f⁻¹(x) = (x + 7)/3 and (5 + 7)/3 = 4.

8 is f(5), evaluating instead of inverting; −2/3 is (5 − 7)/3, a sign slip in the inverse; 1/8 is the reciprocal 1/f(5), not the inverse.

**7. D** — *Domain of a square root with a negative coefficient.* The quantity under the root cannot be negative: 4 − 2x ≥ 0, so −2x ≥ −4, and dividing by −2 reverses the inequality: x ≤ 2. D = (−∞, 2].

[2, ∞) forgets to reverse the inequality when dividing by a negative; (−∞, −2] drops the sign of the 4 when moving it across (−2x ≥ 4); [−2, ∞) makes both of those errors at once.

**8. A** — *Domain is fixed before cancelling.* The domain is decided by the function as given, before any cancelling: the denominator x − 3 is zero at x = 3, so f(3) is undefined and D = ℝ \ {3}. Cancelling produces x + 3, which agrees with f everywhere except at that one missing point.

ℝ ignores the division by zero; ℝ \ {−3} excludes a zero of the numerator instead; ℝ \ {−3, 3} excludes both roots of the numerator, which never restrict a domain.

**9. D** — *Domain and range of a square root function.* The quantity under the root cannot be negative, so x + 6 ≥ 0 and x ≥ −6, giving D = [−6, ∞). Since √(x + 6) ≥ 0, subtracting 4 gives h(x) ≥ −4, so R = [−4, ∞); nothing was multiplied by a negative, so no inequality reverses.

Both brackets are square because x = −6 is a legal input and h(−6) = −4 is actually reached. [6, ∞) has the sign of the boundary wrong, [0, ∞) forgets the −4, and the round brackets wrongly exclude endpoints that are attained.

**10. A** — *Finding and verifying an inverse.* Let y = (3x − 2)/4, switch the letters to get x = (3y − 2)/4, then isolate y: 4x = 3y − 2, 3y = 4x + 2, y = (4x + 2)/3. Checking, f(f⁻¹(x)) = (3·(4x + 2)/3 − 2)/4 = 4x/4 = x, so f⁻¹(x) = (4x + 2)/3 is right.

(4x − 2)/3 loses the sign when the −2 is moved across, 4/(3x − 2) is the reciprocal rather than the inverse, and (3x + 2)/4 never switches x and y at all.

**11. C** — *Domain with a root and a denominator.* Two restrictions apply at once. The root needs x − 1 ≥ 0, so x ≥ 1, and the denominator needs x − 4 ≠ 0, so x ≠ 4. Putting them together gives [1, 4) ∪ (4, ∞), also writeable as x ≥ 1 with x ≠ 4.

[1, ∞) forgets the denominator, ℝ \ {4} forgets the root, and (1, 4) ∪ (4, ∞) wrongly throws out x = 1, where f(1) = 0/(−3) is perfectly defined.

**12. B** — *Square versus round brackets in interval notation.* A square bracket says the endpoint is actually attained, a round bracket says it is not. At x = 3 the absolute value is 0, so h(3) = 2 and the output 2 really is reached, which earns the square bracket. On the other side −∞ is not a number at all: h just keeps falling without ever landing on it, so that end stays round.

The maximum-versus-minimum option invents a rule that does not exist, and the option about h being defined at x = 2 confuses an input with an output.
